# SentinelAI Reviewer

Core components: repository ingestion, AST parsing, semantic chunking, and dependency graph analysis.

See `requirements.txt` and `project/config.py` for runtime settings.

Run the dashboard:

```bash
python -m pip install -r requirements.txt
streamlit run project/ui/dashboard.py
```

